Dans cette version :
1)	On extraire que les diagonales (donc sans leurs sous diagonales).
2)	On utilise la méthode de poids : w(D)=w(D1)+…+(Dn), sans appliquer la méthode de (si < 10puis-5).
3)	Dans la class « InconsistantDiagonales » on chage que la méthode « inconsisanteSimple » car on n’a pas des sous diagonales. Donc le traitement qui a une relation avec eux on ne le fait pas.
4)	On a pas des sous diagonales donc on utilise pas la class « SuppSousDiagonales »
